spring:
  datasource:
    url: jdbc:postgresql:///${SPRING_DATASOURCE_DATABASE_NAME}?cloudSqlInstance=${SPRING_DATASOURCE_INSTANCE_CONNECTION_NAME}&socketFactory=com.google.cloud.sql.postgres.SocketFactory&user=${SPRING_DATASOURCE_USER}&password=${SPRING_DATASOURCE_PASSWORD}
